apiVersion: v1
kind: Pod 
metadata:
  name: my-app
  labels:
      my-app
spec:
  containers:
   - name: my-container
     image: httpd:latest
     ports: 
      - name: http
        containerPort: 80